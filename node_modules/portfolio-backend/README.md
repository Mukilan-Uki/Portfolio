# Backend

This folder contains the Node.js backend for the portfolio.

- `server.js`: Express server that serves the frontend static files and exposes a simple API.
- `package.json`: Backend metadata and dependencies.

## Run

1. `cd backend`
2. `npm install`
3. `npm start`

Then open `http://localhost:3000` in your browser.
